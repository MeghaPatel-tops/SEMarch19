procedure:=>


CREATE PROCEDURE getEmployees()
SELECT * from employee;
CALL getEmployees()


CREATE PROCEDURE getEmpById(id int)
SELECT * from employee WHERE empid=id
call getEmpById(4);

View:=>
CREATE VIEW empview AS SELECT name,email, salary from employee

SELECT salary from empview

CREATE TABLE temptbl (tid int AUTO_INCREMENT PRIMARY KEY,empid int,timeof datetime)

CREATE TRIGGER CREATE TRIGGER empUpdateTrigger AFTER UPDATE
ON employee FOR EACH ROW
INSERT INTO temptbl(empid ,timeof)VALUES(old.empid,now());

DROP TRIGGER empUpdateTrigger


CREATE TRIGGER empUpdateTrigger AFTER UPDATE
ON employee FOR EACH ROW
INSERT INTO temptbl(empid,oldname,newname,timeof)VALUES(old.empid,old.name,new.name,now());


START TRANSACTION;

UPDATE employee set email = "abc@example.com" where empid=4;


SAVEPOINT sp1;
UPDATE   employee set city="daman" where empid=6;


SAVEPOINT sp2;
DELETE from employee WHERE empid=9;
UPDATE employee set city ="daman" where empid=1;


ROLLBACK to sp2;


COMMIT;

==========================pl sql==============================

DECLARE
  message varchar2(100):= 'Hello, World!'; 
  age number:=12;
BEGIN 
  dbms_output.put_line(message); 
  if age >= 18 then
     dbms_output.put_line('eligible'); 
  else
     dbms_output.put_line('not eligible');   
  end if;
END;
/


DECLARE
  message varchar2(100):= 'Hello, World!'; 
  age number:=12;
  i number;
BEGIN 
  dbms_output.put_line(message); 
  if age >= 18 then
     dbms_output.put_line('eligible'); 
  else
     dbms_output.put_line('not eligible');   
  end if;

  for i in 1..10 loop
     dbms_output.put_line('i=' || i);  
  end loop;   
END;


